/**
 * Shared Button `sx` for back-office actions — matches Master → Category modals
 * (AddCategory / EditCategory primary & cancel).
 */
export const masterCategoryContainedButtonSx = {
  textTransform: "capitalize",
  borderRadius: "8px",
  fontWeight: "500",
  fontSize: "13px",
  padding: "12px 20px",
  color: "#fff !important",
};
