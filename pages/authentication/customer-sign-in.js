/**
 * Route used for Next.js; unauthenticated users are rendered via _app → SignIn(portalCustomerLogin).
 * Visiting this URL shows the reservation customer login surface and routes to /customer-portal after sign-in.
 */
export default function CustomerSignInPage() {
  return null;
}
